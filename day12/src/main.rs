struct Line {
    conditions: Vec<usize>,
    record: Vec<char>,
}

fn main() {
    let data = get_formatted_data(&util::get_data("data.txt"));
}

fn get_formatted_data(data: &Vec<String>) -> Vec<Line> {
    let v = Vec::new();

    for i in 0..data.len() {
        let line = data[i];
    }

    return v;
}
